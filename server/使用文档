# 查找占用 3000 的进程并强制杀死
sudo lsof -t -i:3000 | xargs kill -9

重启后端 (server 目录 npm run start:dev)：必须重启，因为加了 Gateway。

启动后台 (admin 目录 npm run dev)：打开浏览器 http://localhost:5173。

启动小程序 (client 目录 npm run dev:mp-weixin)：打开微信开发者工具。